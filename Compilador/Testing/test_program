                                                  /% Group 2 %/

                           /% ------------- Program without errors.  ------------------- %/

    LONGINT a , b , c , d ;
    FLOAT   x,y,z,w;
    LONGINT longint_negative;
    FLOAT   float_negative;

    a = 3_l;
    x = 5.0f+3;
    w =  3.3 +  5.0f+3;
    a = 2_l * 3_l;
    c = 5_l;
    /% b = 2_l - a / c ; %/             /%ARREGLAR %/
    w = 4.5 + 3.3 + 4.2;

    float_negative = -7.3;
    longint_negative = -715_l;
    d = 2_l - -3_l;

    FLOAT p1,p2;
    PROC procedure_1 (FLOAT p1) NA = 1_l , SHADOWING = TRUE
    {
            IF (e != p2)
               OUT ("cadena de caracteres");
            ELSE
                float_negative = float_negative - 1.;
            END_IF;
    };

    PROC procedure_2 (FLOAT p1, FLOAT p2) NA = 1_l , SHADOWING = TRUE
    {
        FLOAT e;
        e = 2.;
        WHILE (p1 <= 12.) LOOP
        {
            e = e + 1.;
            IF (e != p2)
               OUT ("cadena de caracteres");
            ELSE
                float_negative = float_negative - 1.;
            END_IF;
        };
    };

    procedure_2(x,y);





$